# Keep implementation plans out of the repository

For this prototype, when the user requests a fast implementation and explicitly says not to write a plan in the repository, keep planning internal. Do not create `docs/superpowers/plans/*`; feature documentation and correction memory remain separate project requirements.
